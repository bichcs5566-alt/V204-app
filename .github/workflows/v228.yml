name: run-v228

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install pandas numpy

      - name: Run strategy
        run: python v228_main.py

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: v228_result
          path: |
            v228_nav.csv
            v228_summary.csv
