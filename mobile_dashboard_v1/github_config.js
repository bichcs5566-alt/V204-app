// 可留空白預設，不放真正 token
const GITHUB_CONFIG = {
  owner: "bichcs5566-alt",
  repo: "V204-app",
  branch: "main",
  workflows: {
    positionWriteback: "v2_position_writeback.yml",
    pipeline: "v2_pipeline.yml"
  }
};
